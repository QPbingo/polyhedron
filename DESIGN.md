---
name: 多面体 · 简约会话工作台
description: 会话列表与终端双栏，设备与六套配色收进设置。默认电光蓝，配色仍可选择。
colors:
  background: "#f1f6ff"
  surface: "#ffffff"
  text: "#1e293b"
  muted: "#536176"
  border: "#e6edf5"
  action: "#2455e6"
  action-soft: "#e9f0ff"
  teal: "#00796b"
  terminal: "#101e3d"
  terminal-text: "#e3ebf7"
  terminal-muted: "#a1b2c9"
rounded:
  compact: "6px"
  action: "8px"
  terminal: "0px"
  dialog: "14px"
---

# 多面体 · 简约工作台

## 当前方向

用户最新反馈是“整体界面还是有点复杂，尽量简约”。主流程集中为：展开项目 → 找到会话 → 接管 → 在终端继续工作。最新侧栏参考实际查看的 Orca 本机窗口：上方功能入口，下方 Projects 和层级列表。保留此前要求的鲜亮配色，把颜色集中用于品牌、主按钮、当前会话与状态。

设计已获用户确认，并已迁移至 frontend/ 正式应用，通过 backend/ 接入真实目录、持久化会话与原生 CLI。demos/ 保留此前静态原型。第一版纸面、石墨、像素方案已被否定；第二版曾参照 telemedicine 页，但其宣传区域和卡片层级已由本次简化取代。

## 布局与信息层级

桌面默认左侧 248px（小桌面 224px）会话栏，右侧伸缩终端区，高度使用 100dvh。左侧上方是搜索、全部会话、待处理、已结束功能；下方是 Projects 区域和可折叠的项目/会话树，底部保留设置/使用指南。项目行仅显示单行名称，隐藏目录副标题。鼠标悬浮或键盘聚焦时显示唯一的“…”操作入口，菜单包含“新建会话”和“项目设置”；触控设备保留可见入口。完整目录保留在悬停提示和项目设置中。按用户最新八处逐项标注，移除品牌及其演示徽标、右侧会话标题、项目路径、操作权横栏、接管按钮、重复活动状态与底部主机/中断栏。右侧从顶部直接显示终端，右侧外围留白和终端圆角均为 0，终端贴齐右侧区域四边，占满视口高度；保留终端内容本身的内边距。

去掉顶部宣传标题、全局导航、账号装饰、独立主机卡片、外围卡片容器、常驻配色面板及宣传页脚。正式终端不额外添加重复的欢迎区和路径；原生 CLI 自身输出保持原样。

设备、六套配色、终端字号放入设置；正式版显示真实主机连接，并提供配对、登录撤销、历史配额和防休眠设置。会话详情入口移到终端工具栏；主机、连接、项目、操作权、接管控制与中断任务在详情中按需显示。选择会话不会自动接管，原有操作权校验保留。专注模式隐藏侧栏。搜索快捷键会退出专注模式并聚焦搜索。

列表与终端独立滚动；1440×1000、1280×800 和 1100×800 下主界面无需页面纵向滚动。窄窗口使用上下重排作为兜底，移动端仍不是产品验收范围。

## 配色

| 配色 | 品牌主色 / 辅色 | 主按钮 | 页面底色 | 终端底色 | 气质 |
| --- | --- | --- | --- | --- | --- |
| 电光蓝（默认候选） | `#3265FF` / `#16D4E0` | `#2455E6` | `#F1F6FF` | `#101E3D` | 清亮、科技 |
| 鸢尾紫 | `#8B4AFF` / `#FFAB8F` | `#7434DC` | `#F7F3FF` | `#261B3B` | 灵感、创作 |
| 珊瑚橙 | `#FF703D` / `#FB508C` | `#C04018` | `#FFF6F0` | `#302023` | 温暖、活力 |
| 翡翠绿 | `#00B881` / `#BCE849` | `#087B58` | `#F0FAF5` | `#132E28` | 清新、轻快 |
| 玫瑰粉 | `#F34296` / `#48BFFF` | `#C82672` | `#FFF3F8` | `#2C1B30` | 明快、个性 |
| 原版蓝青（对照） | `#4F8CFF` / `#20B8BD` | `#2D68D6` | `#F5F8FD` | `#152238` | 柔和、克制 |

正式主题变量见 `frontend/src/palettes.css`，原型对应 `demos/palettes.css`。页面使用白色侧栏与轻微主题底色，没有大面积背景渐变或卡片阴影。主按钮和品牌使用实色。状态语义色不随配色改变。原版蓝青现表示原配色，不恢复旧布局。

配色切换保留当前会话和草稿，允许本地存储时记住偏好；`?palette=iris` 等查询参数优先于存储。六套选择入口都在设置中。

## 字体与组件

沿用 Inter 优先的系统无衬线字体栈和中文系统字体回退，无远程字体请求；终端与路径使用系统等宽字体。左侧会话标题 13px，常用控件 12–13px，终端默认 14px，可设置 12–20px。

主界面只保留一道侧栏分隔线；会话选中通过浅主题底色与文字强调。控件主要使用 6–8px 圆角，终端无圆角，对话框 14px；表单子控件沿用 9–13px 圆角。对话框保留遮罩、焦点约束和 Escape 关闭，不使用装饰性背景模糊。

## 验证与边界

正式实现的测试、截图和外部验收项见 [交付验证记录](docs/implementation/VERIFICATION.md)。以下为此前静态设计阶段记录，不代表生产性能或跨设备验收。

9 项状态模型测试和 Chrome 浏览器回归检查通过，覆盖初始化、筛选、接管、文本安全显示、草稿保留、模拟重连、审批、新建、搜索、离线、详情、专注模式、帮助、设置中的六套配色和字号。桌面、紧凑桌面与窄窗口最新截图在 `demos/review-projects/`，包含用户反馈视口 1231×805。

本轮浏览器检查了默认主题 7 组文字配对，均达到 4.5:1。之前六套颜色的 78 组配对记录仍在 `demos/review-palettes/contrast.json`；本轮未把它当作全页面无障碍认证。未验证真实 CLI、跨设备通信、Safari 或完整读屏操作。

设计扫描器因缺少 HTML/CSS 解析依赖，降级为正则扫描，不能验证实际样式或对比度；结果主要提示既有 Inter 字体、旧设计缓存与当前颜色/圆角的差异。实际布局由 Chrome 检查。`.impeccable/design.json` 是旧缓存，本轮没有扩展为设计缓存迁移任务。

历史截图保留在 `demos/review-v2/`、`demos/review-palettes/`、`demos/review-simple/`、`demos/review-minimal/`，不代表当前布局。

## 项目与目录绑定

项目是独立实体，包含稳定 ID、名称、规范化目录和执行主机；会话只保存 `projectId` 引用，主机和目录从所属项目读取。会话标题、Agent 和项目名称都不充当目录身份。

新建会话仅通过具体项目的悬浮操作菜单进入，自动绑定该项目，提交时读取明确项目 ID。移除全局新建入口和项目选择器，表单只展示所属项目名称与目录。目录也在会话详情中显示。同名但不同目录可以分别添加；相同主机的同一规范化目录不可重复添加；无效项目、离线项目不能创建会话。正式项目与会话由执行主机 SQLite 持久保存，刷新后仍存在；新增目录须存在并通过主机授权根目录的 realpath 校验。静态 Demo 保留刷新重置的演示行为。

筛选与搜索保留项目分组，支持项目名称、完整路径、~/ 路径、会话标题和 Agent 名称。筛选时展开匹配分组；清空筛选后恢复折叠偏好。没有会话的项目仍可从菜单创建第一个会话。

Orca 仅提供本次侧栏层级与密度参考，没有引入其任务、自动化或 worktree 功能。产品已有的无品牌侧栏、六套配色、右侧满屏终端保持不变。

## 原生终端输入对比

Codex 的 `›` 和 Claude 的 `❯` 用户提示行添加浅色底及左侧细线，模型输出保持原生深色背景。软换行延续输入底色；滚动、快照重绘和全屏缓冲切换会重新定位。采用不拦截鼠标事件的显示层，保留 CLI 字符、选区、权限审批、输入协议和原有颜色。该效果基于提示符识别，不把无法识别的 CLI 输出猜成聊天角色；独立硬换行或未来 CLI 改变提示符时需适配。
