# 简约工作台检查

2026-09-08，独立无头 Chrome，实际页面截图，减弱动效模式。

- `desktop.png`：1440×1000；`desktop-1280.png`：1280×800；`compact-desktop.png`：1100×800。主界面与终端输入框在视口内，页面无需纵向滚动。
- `settings.png`：设备、字号与六套配色集中在设置；`new-session-dialog.png`：新建演示会话。
- `narrow-760.png`、`narrow-390.png`：上下重排兜底，无页面横向溢出；终端字号已在回归过程中改为 18px。
- 自动化回归覆盖页面初始化、筛选、接管、文本安全显示、草稿保留、设置中的模拟断线/重连、审批、新建、搜索、离线、详情、专注模式、指南、六套配色和字号。无页面脚本错误。
- 默认电光蓝 7 组文字色值配对 ≥4.5:1；6 项状态模型测试通过。
- 设计扫描器缺少解析依赖，降级为正则扫描；这不是完整设计或无障碍认证。Safari、真实终端和跨设备通信未验证。

复现：按上级 README 配置 Playwright 与 Chrome，执行 `node demos/browser-check.cjs`。
